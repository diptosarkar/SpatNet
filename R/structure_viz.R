#' A trial Function
#'
#' This function allows you to express your love of Hello World
#' @param: igraph object with V(g)$X and V(g)$Y having the X and Y co-ordinates of each node.
#'         resolution specifies the grouping of points for numbering. Defaults to NA and numbering cannot be performed
#'         num specifies whether points should be aggregated and numbered. Requires resolution to be specified
#'         jitter specifies if points on the visualization should be jittered to get an idea of how many points ovelap. Defaults to TRUE
#' @keywords: spatial clustering
#' @export
#' @examples
#' structure_viz()


structure_viz<-function(graph, resolution=NA, num=TRUE, jitter=TRUE){

}
